name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      # Mesma versão do ruff travada em requirements-dev.txt
      - run: pip install "$(grep '^ruff==' requirements-dev.txt)"
      - run: ruff check .
      - run: ruff format --check .

  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: transparencia
          POSTGRES_PASSWORD: transparencia
          POSTGRES_DB: transparencia_br
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U transparencia -d transparencia_br"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      DATABASE_URL: postgresql+asyncpg://transparencia:transparencia@localhost:5432/transparencia_br
      TRANSPARENCIA_API_KEY: test_key

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: pip install -r requirements-dev.txt && pip install --no-deps -e .
      - run: alembic upgrade head
      - run: pytest --cov=app --cov-report=term-missing --cov-fail-under=70
